# 新闻抓取系统依赖包

# 核心依赖
aiohttp>=3.8.0          # 异步HTTP客户端
beautifulsoup4>=4.11.0  # HTML解析
flask>=2.0.0           # Web框架（如果需要独立API服务）
sqlalchemy>=1.4.0      # 数据库ORM（可选）

# 数据处理
pandas>=1.5.0          # 数据分析（可选）
numpy>=1.21.0          # 数值计算（可选）

# 日期时间处理
python-dateutil>=2.8.0

# JSON处理（Python内置，但有些环境可能需要）
# json

# 正则表达式（Python内置）
# re

# 异步支持（Python 3.7+ 内置）
# asyncio

# 路径处理（Python 3.4+ 内置）
# pathlib

# HTTP状态码
requests>=2.28.0       # 用于API客户端测试

# 开发和测试依赖
pytest>=7.0.0          # 测试框架
pytest-asyncio>=0.21.0 # 异步测试支持

# 可选：更好的日志
colorlog>=6.7.0        # 彩色日志

# 可选：配置文件支持
pyyaml>=6.0            # YAML配置文件
python-dotenv>=1.0.0   # .env文件支持

# 可选：任务调度
schedule>=1.2.0        # 简单的任务调度

# 可选：更好的HTTP头处理
fake-useragent>=1.4.0  # 随机User-Agent

# 可选：数据验证
pydantic>=1.10.0       # 数据验证和序列化
